.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install expan

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv expan
    $ pip install expan
